---
name: Validate JSONs

on  : [pull_request]

jobs:
  verify-json-validation:
    runs-on: ubuntu-latest
    steps:
    - name: Validate YAML
      uses: brittonhayes/validate-yaml
      with:
        schemaPath: schema.json
        files: |
          recipes/*
